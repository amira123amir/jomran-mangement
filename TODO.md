- [ ] فهم المطلوب: عند الضغط على شرائح/أزرار تصنيفات قسم المشتريات (إجمالي الطلبات، بانتظار التعيين، …) يظهر/يتم تطبيق عدد الطلبات المطابق لكل حالة (بالـ count).
- [ ] فحص أين يتم عرض شريط التصفية الحالية داخل ProcurementWorkspace.
- [ ] تعديل/تحسين UI إن لزم: جعل الأزرار تعرض الأرقام صحيحة وتكون قابلة للضغط لتحديث الفلتر.
- [ ] التأكد أن العدّ يعتمد على orders المصرّح للمستخدم فقط (authorizedOrders) وأنه لا يتأثر بأرشفة/إجراءات أخرى.
- [ ] تشغيل المشروع (npm install / npm run dev) واختبار الضغط على: كل الطلبات + بانتظار التعيين.

